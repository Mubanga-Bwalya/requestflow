/** Default rows per page for list views (matches backend default). */
export const LIST_PAGE_SIZE = 20;

/** Dashboard / workspace preview — enough for “do next” without loading full history. */
export const WORKSPACE_PREVIEW_LIMIT = 30;

/** Notification panel page size. */
export const NOTIFICATION_PAGE_SIZE = 15;
