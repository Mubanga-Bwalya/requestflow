import type { Department } from "./department"; import type { Milestone } from "./milestone"; export type AssignmentMember = { id: string; name: string; }; export type Assignment = { id: string; title: string; relatedRequest: string; department: Department; status: "ASSIGNED" | "IN_PROGRESS" | "READY_FOR_REVIEW" | "COMPLETED"; progress: number; deadline: string; milestones: Milestone[]; members: AssignmentMember[]; };
